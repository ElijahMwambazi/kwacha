[project]
name = "kwacha-backend"
version = "0.1.0"
description = "FastAPI backend for Kwacha price tracker"
requires-python = ">=3.11"
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]