# Kwacha Backend

FastAPI backend for items, price observations, basket calculations, imports, indicators, and future predictions.

## Run locally

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
